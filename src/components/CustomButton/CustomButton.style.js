import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: { margin: 10, padding: 10, backgroundColor: '#4F1C57', alignItems:'center', borderRadius: 10 },
    buttonText: { fontSize: 18, fontWeight: '400', color: 'white' }
})